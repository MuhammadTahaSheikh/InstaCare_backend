# Copy to .env.local for development on your Mac
# This overrides Hostinger credentials in .env
# If port 5001 is in use, change to 5002 (only one backend per port)

PORT=5001
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=bestechcare
