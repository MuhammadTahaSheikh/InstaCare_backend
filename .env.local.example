# Optional local overrides. DB credentials come from .env (Hostinger).
# If port 5001 is in use, change to 5002 (only one backend per port)
PORT=5001

# To use local MySQL instead of Hostinger, uncomment:
# DB_HOST=localhost
# DB_USER=root
# DB_PASSWORD=
# DB_NAME=bestechcare
